# Local assets

Place large or machine-specific assets here when they are needed for local
development but should not be uploaded to GitHub.

Examples:

- extracted PakV4/PSS packages
- MovieEditor cache files
- local screenshots or captures
- proprietary game assets copied from `C:\SeasunGame`

Only this README is tracked. Everything else under `assets/` is ignored.

